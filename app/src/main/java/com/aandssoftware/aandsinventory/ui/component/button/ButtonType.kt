package com.aandssoftware.aandsinventory.ui.component.button

/**
 * Type of button depending on their priority.
 */
enum class ButtonType {
    // Use for primary actions on the screen. The primary button contains an action that is important,
    // but isn't the single focus of the screen.The primary button has less emphasis than the CTA button,
    // but more than the secondary button
    PRIMARY
}
